/**
 * Platform Admin Home-view Router Forwarder
 * Fully independent section, modularly organized, independently deployable.
 */

import React from 'react';
import PlatformRootHub from '../platform';

export default function HomeView() {
  return <PlatformRootHub />;
}
